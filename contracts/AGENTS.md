# contracts

JSON Schema for codegraph symbol rows persisted by `jeryu-codegraph`.
Changes to `contracts/codegraph.schema.json` stay in this cell and are
routed through `just check` and `just score`.
